DROP TABLE attfile;
DROP SEQUENCE attfile_seq;

CREATE SEQUENCE attfile_seq start with 1 NOCACHE;
CREATE TABLE attfile (
	fid NUMBER,
	bid NUMBER,
	img NVARCHAR2(1024) NOT NULL,
	thumb NVARCHAR2(1024) NOT NULL
);

ALTER TABLE attfile ADD CONSTRAINT attfile_fid_PK PRIMARY KEY(fid);
ALTER TABLE attfile ADD CONSTRAINT attfile_bid_FK FOREIGN KEY(bid) REFERENCES board(bid);